# ✅ צ'קליסט לפני העלאה לענן (GitHub Pages)

> **חשוב!** לפני שמעלים את "אתר משופר" לרשת — חובה לטפל בנטרול ה-Firebase
> שעשינו לצורך הבדיקות המקומיות, אחרת הסנכרון לא יעבוד כשנרצה שכן יעבוד.

## 🔴 הדבר הקריטי: להחזיר את Firebase (אם רוצים סנכרון בענן)

בזמן הבנייה **נטרלנו** את Firebase כדי שהעותק המקומי לא יסנכרן בטעות לנתונים החיים.
הנטרול נעשה בדגל `window.IMPROVED_SITE_SANDBOX`. לפני העלאה — להחליט:

- [ ] **אפשרות א' — סנכרון מול אותו פרויקט (אותם נתונים של האתר החי):**
      להסיר את השורה `<script>window.IMPROVED_SITE_SANDBOX = true;</script>` מ-`index.html`
      (או לשנות ל-`false`). אז ההתחברות עם Google תסנכרן לאותו ענן כמו האתר החי.
- [ ] **אפשרות ב' — פרויקט Firebase נפרד (נתונים נפרדים לאתר המשופר):**
      ליצור פרויקט Firebase חדש, לעדכן את `js/firebase-config.js` עם הפרטים החדשים,
      ואז להסיר את דגל הסנדבוקס.
- [ ] **אפשרות ג' — להשאיר מקומי בלבד (בלי סנכרון בענן):**
      להשאיר את הדגל כמו שהוא. האתר יעבוד, הנתונים יישמרו רק בדפדפן של כל מכשיר.

**איפה הנטרול בקוד (להסרה/שינוי):**
- `index.html` — השורה `window.IMPROVED_SITE_SANDBOX = true;` (ליד טעינת הסקריפטים).
- `js/firebase-sync.js` — שני guards שבודקים `IMPROVED_SITE_SANDBOX` (ב-`isConfigured()` וב-`setup()`).
  אפשר להשאיר אותם בקוד (הם פעילים רק כשהדגל דלוק) — מספיק לכבות את הדגל ב-index.html.

## שאר הצעדים להעלאה
- [ ] ליצור ריפו GitHub חדש ונפרד (לא לדחוף לריפו של האתר החי `gagula22/hamachberet-sheli`).
- [ ] להפעיל GitHub Pages על הריפו החדש → מתקבלת כתובת ציבורית עצמאית.
- [ ] לבדוק `sw.js` (Service Worker) — שהנתיב מתאים לכתובת החדשה.
- [ ] בדיקה אחרי העלאה: טעינה נקייה, הבוט עובד, וסנכרון (אם נבחרה אפשרות א'/ב').

---
*נוצר אוטומטית בזמן בניית האתר המשופר — אל תמחק עד שמעלים לענן.*
