# פרומפט סיום לסוכן — "המחברת שלי"

> העתק את כל הבלוק שמתחת לקו והדבק לסוכן **בסוף כל משימה/שיחה** (או כשעוצרים באמצע).
> מטרתו: שום ידע לא הולך לאיבוד — הסוכן הבא קורא את התיעוד ויודע להמשיך בלי לשאול שאלה.

---

סיים את הסשן לפי הפרוטוקול הבא, שלב אחרי שלב, בלי לדלג. המטרה: הסוכן הבא לא רואה את
השיחה הזו — כל מה שקרה כאן חייב לחיות במסמכים ובקומיטים.

## 1. עצור ועשה חשבון — מה באמת קרה בסשן

- הרץ `git status --short` + `git log --oneline -10` וענה לעצמך: מה שיניתי? מה קומט?
  מה נדחף? האם נשאר משהו לא-מקומט או לא-דחוף?
- עבודה שהושלמה אך לא קומטה — קמט אותה עכשיו (קומיט נפרד לכל נושא, `git add` ממוקד).
- עבודה שנקטעה באמצע — אל תקמט חצי-עבודה שוברת; או השלם אותה, או בטל אותה נקי,
  ובכל מקרה תעד את מצבה המדויק ברשומת ה-handoff (שלב 4).

## 2. נקה אחריך

- מחק נתוני בדיקה שיצרת (נושאים/משימות/הקלטות בדיקה ב-Store/IndexedDB של ה-preview).
- ודא שלא נשארו קבצים זמניים/עזר בתיקיית הפרויקט שלא נועדו להיכנס לריפו.
- אין שינויים "מיותמים" ב-working tree: כל מה ש-`git status` מציג הוא או רעש CRLF ידוע
  או משהו שאתה יודע להסביר. משהו לא מוסבר? ברר לפני שאתה סוגר.

## 3. סגור את לולאת האימות

- כל מה שנדחף — אומת מול האתר החי (fetch עם פרמטר אקראי; המתן ~דקה ל-GitHub Pages)?
- כל ?v=N הוקפץ לכל JS/CSS ששונה? (זכור: ה-SW מגיש עריכה ישנה באותו URL.)
- בדיקה חיה אחרונה ב-preview: הפיצ'רים שנגעת בהם עובדים, מסכים שכנים לא נשברו,
  אפס שגיאות קונסול.
- משהו לא אומת עד הסוף? אל תציג אותו כגמור — סמן אותו במפורש כ"פתוח/לא-מאומת" בתיעוד.

## 4. כתוב את רשומת ה-Handoff — לב הסיום

הוסף רשומה **בראש** היומן ב-`UPDATES.md` לפי התבנית שבפרוטוקול. הרשומה חייבת לענות
במדויק על: **מה** בוצע · **למה** (הבעיה) · **איך** (הפתרון, כולל מלכודות שהתגלו) ·
**מתי** (תאריך + קומיטים) · **ואיפה עצרנו**.

סעיף "איפה ממשיכים / פתוח" הוא החשוב מכולם — כתוב אותו כאילו אתה משאיר פתק למחליף שלך:
- מה עובד ומאומת (עם קומיט וגרסאות v).
- מה נשאר פתוח: משימות שלא הושלמו, באגים ידועים, רעיונות שהוצעו למשתמש וטרם אושרו.
- אם נעצרת באמצע משהו: מה בדיוק המצב, מה הצעד הקונקרטי הבא, אילו קבצים מעורבים,
  ומה כבר ניסית שלא עבד (כדי שהבא לא יחזור על אותה דרך).
- הנחיות/העדפות שהמשתמש נתן בסשן ורלוונטיות להמשך — צטט אותן.

## 5. עדכן את שאר המסמכים לפי טבלת סעיף 4 של AGENT-PROMPT.md

- `PROJECTS.md` — כרטיס ה-P-xx: שורת "בוצע" (תאריך+קומיט) לכל יכולת חדשה; הוסף משימות
  פתוחות חדשות שהתגלו.
- `ARCHITECTURE.md` — כל שינוי מבני, מלכודת חדשה או כלל-מניעה חדש → הסעיף הרלוונטי
  (או סעיף חדש בסגנון §14–§15). תיעוד מלכודת = חשוב לא פחות מהתיקון עצמו.
- `AGENT-PROMPT.md` — רק אם נולד כלל עבודה חדש שכל סוכן עתידי חייב להכיר.

## 6. קומיט תיעוד אחרון + דחיפה + אימות

- קמט את עדכוני המסמכים (קומיט docs ממוקד), `git push origin main`, וודא שהדחיפה עברה.
- נעילת git תקועה (`.lock`)? ודא שאין תהליך git רץ — ורק אז הסר אותה.

## 7. דיווח סיום למשתמש

סכם בעברית, קצר וברור:
- מה בוצע בסשן (עם מספרי קומיטים) ומה אומת חי.
- מה נשאר פתוח ומה הצעד הבא המומלץ.
- היכן התיעוד המלא (הרשומה החדשה ב-UPDATES.md).

## בדיקת-עצירה אחרונה לפני שאתה מסיים

שאל את עצמך: **"אם סוכן חדש יקרא מחר רק את הרשומה שכתבתי ב-UPDATES.md — האם יידע
להמשיך בדיוק מהנקודה הזו, בלי לשאול אף שאלה ובלי לשחזר שום דבר מהשיחה?"**
אם התשובה אינה "כן" מוחלט — חזור לשלב 4 והשלם את החסר.
