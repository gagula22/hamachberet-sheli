# מפת אחריויות — "אתר משופר עם fable 5"

> **מסמך מסירה לכל סוכן/מפתח עתידי.** כל יחידה כאן היא אחריות עצמאית: תיקייה משלה,
> CSS משלה, namespace משלה. עריכה של אחריות אחת לעולם לא דורשת נגיעה באחרת.
> לפני כל שינוי: קרא את `ARCHITECTURE.md` (מפת הליבה) ואת הסעיף הרלוונטי כאן.
>
> **כללי ברזל:**
> 1. נגיעות מאושרות בקבצים קיימים הן רק: שורת SECTIONS ב-`js/app.js`, תגיות `<script>/<link>`
>    ב-`index.html` (עם הקפצת `?v=N`), רשומת מפתח ב-`store-schema.js` + עדכון האסרציה
>    ב-`firebase-sync.js`, אריח ב-`stickers.js` (לכלים בלבד), ורשומות ידע ב-`assistant/knowledge.js`.
> 2. כל קובץ JS עובר `node --check` לפני קומיט. קומיט אחד לכל פיצ'ר/תיקון.
> 3. אימות חי על `http://localhost:7795` — **חובה להריץ עם `python server.py 7795`**
>    (לא `http.server` — הפרוקסי של התמלול יושב בשרת).
> 4. האתר הזה מקומי בלבד. ⛔ אסור לפרסם לאינטרנט או לגעת באתר המקורי (`מחברת/`) עד הוראה מפורשת.
> 5. `window.IMPROVED_SITE_SANDBOX=true` ב-index.html מנטרל Firebase — לא להסיר (ראה DEPLOY_CHECKLIST.md).

---

## חלק א' — תשתית והפעלה

### A1. שרת מקומי + פרוקסי תמלול — `server.py`
- **תפקיד:** מגיש את האתר (סטטי) + מסלול `/vt-proxy/*` שמעביר בקשות תמלול ל-Cloudflare Worker
  שרת-אל-שרת (עוקף CORS שמוגבל ל-gagula22.github.io).
- **נקודת כניסה:** `python server.py 7795` (מוגדר גם ב-`.claude/launch.json` בתיקיית העבודה).
- **נקודת סיום:** שרת חי על 7795; הפרוקסי מחזיר את תשובת הענן כמו-שהיא.
- **קריטי לדעת:** Cloudflare Bot-Fight חוסם User-Agent של python (שגיאה 1010) — הפרוקסי שולח
  כותרות-דפדפן. גוף בקשה chunked נתמך ידנית. הדפסות קונסולה — ASCII בלבד (קונסולת Windows cp1255).
- **סטטוס:** ✅ נמדד חי: נתח 90 שניות ≈ 5.2 שניות הלוך-חזור דרך הפרוקסי.

### A2. סכימת נתונים — `js/store-schema.js` (+שער בטיחות ב-`js/firebase-sync.js`)
- **תפקיד:** מקור אמת יחיד לכל מפתחות ה-Store. הוספת מפתח = שורה כאן + עדכון מערכי
  `sub`/`main` באסרציה של firebase-sync (נכשל בכוונה אם שכחת).
- **מפתחות שנוספו בפורק הזה:** `readingList`, `flashcards` (subcol) · `eisenhower`, `weeklyReviews` (maindoc).
- **נקודת סיום:** Store.get(key) מחזיר את ברירת המחדל; באתר חי הסנכרון נגזר אוטומטית.

### A3a. צינור תמונות משותף — `js/components/editable/utils.js` (`EditableUtils.compressImage`)
- **תפקיד:** המשפך היחיד לכל תמונה שמודבקת/נבחרת במחברת, בהערות ובסקירה השבועית.
- **מדיניות איכות (תוקן לפי משוב — הדבקות יצאו מטושטשות):** ללא הקטנה עד 2560px; תמונה שלא
  צריכה הקטנה ועד ~2MB נשמרת **כמו שהיא** (אפס איבוד — קריטי לצילומי מסך עם טקסט); קידוד-מחדש
  רק לענקיות — WebP 0.92 (נפילה ל-JPEG 0.92); התוצאה לעולם לא גדולה מהמקור.
- **לתשומת לב בפריסה עתידית:** תמונות גדולות יותר מקומית = מסמכי topics גדולים יותר בענן;
  firebase-sync כבר מתגונן עם `_sizeSafeTopic`.
- **איכות גם בייצוא:** Word משבץ את ה-base64 המאוחסן כמו-שהוא (קידוד-מחדש רק לתמונות מכתובת
  חיצונית — JPEG 0.95). PDF (`components/html-to-pdf.js`): **scale אדפטיבי** — 3× כשגובה
  התוכן ×3 ≤ 30,000px (תקרת canvas בדפדפן ~32,767), אחרת 2×; פרוסות JPEG 0.95. אומת:
  מסמך קצר→3, מסמך 23,000px→2.

### A3. עוזר ניווט משותף — `js/components/topic-open.js` (`window.TopicOpen`)
- **תפקיד:** `TopicOpen.open(topicId, name?)` — פותח נושא מחברת מכל מקום באתר.
- **איך:** מנווט ל-#/notebook וקורא ל-`window._nbWikiClick` (מנגנון שהמחברת עצמה חושפת), עם המתנה
  קצרה עד שהמסך נבנה. קריאה בלבד — תלות רכה.
- **בשימוש אצל:** backlinks, highlights, graph, doc-scan, onthisday, sketch, flashcards.

---

## חלק ב' — אחריויות שנבנו בפורק הזה (לפי סדר בנייה)

### B1. הבוט (שדרוג) — `js/views/assistant/{knowledge,engine,ui}.js`
- **תפקיד:** עונה על כל שאלה על האתר (מה זה/איפה/איך), מחפש בתוכן האישי, ועונה תשובות מחושבות.
- **נקודת כניסה:** כפתור 💬 צף / מסך "עוזר חכם" / `AsstEngine.answer(q)`.
- **נקודת סיום:** `{kind, text, results[]}` — results הם צ'יפי ניווט.
- **חוזה תחזוקה:** כל פיצ'ר חדש באתר חייב להוסיף רשומת HELP ב-knowledge.js (id/cat/keys/q/a/where/route)
  ולעדכן את רשימת ה-nav ב-`overview()` שב-engine.js. המנוע מבין הטיות עברית (stripPref+sharesPrefix).
- **סטטוס:** ✅ 67+ רשומות, מכסה את כל 22 המסכים.

### B2. ערכת נושא + הגדרות — `js/features/theme/` + `js/views/settings/` + `css/features/theme-dark.css`
- **כניסה:** `theme/boot.js` רץ סינכרונית ב-`<head>` לפני ה-CSS (קורא mirror מ-localStorage
  `mahberet.theme`) — בלי הבזק. `window.Theme.set('cream'|'dark'|'auto')`, `setFontSize('s'|'m'|'l')`.
- **סיום:** `data-theme`/`data-fs` על `<html>`; אירוע `themechange`; settings.theme ב-Store (Store מנצח אחרי טעינה).
- **קריטי:** כל התאמות הכהה רק ב-theme-dark.css (טוקנים + דריסות לצבעים קשיחים). דף העורך וגוף
  ההערה נשארים בהירים בכוונה (טקסט צבוע-ידנית חייב להישאר קריא).
- **נקודת הרחבה:** `window.SETTINGS_CARDS` — מערך פונקציות-רינדור; כל מודול יכול לדחוף כרטיס להגדרות.
- **סטטוס:** ✅ 22 מסכים נבדקו בכהה.

### B3. לוח תובנות — `js/views/insights/{index,charts}.js` (view `insights`)
- **כניסה:** כניסה למסך → `ensureChartJs()` טוען עצל את `js/vendor/chart.umd.min.js` (Chart.js 4.4.3, מקומי).
- **סיום:** 5 גרפים (מצב רוח 30י, הרגלים 8ש, הוצאות-לפי-קטגוריה, הכנסות/הוצאות 6ח, מים+שינה 14י).
- **קריטי:** אין unmount hook באפליקציה → כל רינדור הורס גרפים קודמים; ה-subscribe בודק
  `document.contains` ומתנתק לבד; בנייה מחדש ב-`themechange`. צבעים נקראים מטוקני CSS בזמן ריצה.
- **סטטוס:** ✅ אפס דליפות אחרי 5 סבבי ניווט.

### B4. חיפוש מהיר Ctrl+K — `js/features/palette/index.js` (`window.Palette`)
- **כניסה:** Ctrl/Cmd+K מכל מקום (capture), או `/` מחוץ לשדות קלט.
- **סיום:** ניווט/פעולה ונסגר. מקורות: `App.sections` החי + `AsstEngine.searchContent` (תלות רכה) + פעולות מהירות.
- **קריטי:** Esc סוגר עם stopPropagation (לא מפיל את מצב-המיקוד של המחברת).

### B5. קול — `js/features/voice/{dictation,memos,transcribe}.js` (views: `voice`)
- **הכתבה (dictation):** כפתור 🎤 מוזרק לסרגל המחברת ב-MutationObserver (אפס עריכה במחברת),
  Web Speech he-IL, טקסט נכנס ב-execCommand → undo/שמירה של העורך חינם.
- **הקלטות (memos):** MediaRecorder (timeslice 1s — אין מגבלת אורך; שעה וחצי ≈ 20MB), אחסון
  ב-IndexedDB **משלו** `hamachberet-voice` (לא ב-Store — אודיו לא מנפח גיבוי/סנכרון). טיימר H:MM:SS.
- **תמלול (transcribe):** כפתור 📝 בכל שורה. סדר יעדים: `/vt-proxy` (localhost) → Worker ישיר
  (כש-github.io) → Whisper מקומי בדפדפן (נתחי 5 דק'). ענן = נתחי 90ש **במקביל ×3**
  (`cloudChunkedParallel`) — נמדד: 4.5 דק' אודיו ב-4.5 שניות; שעה וחצי ≈ פחות מ-2 דקות.
  בסיום — Word יורד אוטומטית (📄 לפתיחה חוזרת; התמלול נשמר על הרשומה).
- **סטטוס:** ✅ צינור מלא אומת חי כולל ענן-דרך-פרוקסי.

### B6. גיבוי גרסאות — `js/features/autobackup/index.js` (`window.AutoBackup`)
- **כניסה:** אוטומטית פעם ביום אחרי `Store.ready()`; ידנית "גבה עכשיו" בכרטיס שבהגדרות.
- **סיום:** צילום מלא ב-IndexedDB `hamachberet-backups` (14 אחרונים). שחזור = צילום-בטיחות →
  `Store.importJSON(File)` → reload.
- **סטטוס:** ✅ סבב שחזור מלא + גיזום ל-14 אומתו.

### B7. קישורים-חוזרים — `js/features/backlinks/index.js` (`window.Backlinks`)
- **כניסה:** אוטומטית — MutationObserver על `#view` מזהה `.nb-stage` (נושא פתוח במחברת).
- **סיום:** פאנל "🔗 מי מקשר לכאן" בתחתית העמוד: קישורים מפורשים (`data-tid`) + אזכורי-שם.
  לחיצה → TopicOpen. אין נושא/אין מקשרים → אין פאנל.
- **קריטי:** הנושא הפעיל מזוהה מ-`.nb-topic.active` + אימות מול `.nb-title`. דיבאונס 300ms.

### B8. מפת קשרים — `js/views/graph/index.js` (view `graph`)
- **כניסה:** מסך "🕸️ מפת קשרים". בונה גרף מ-topics: קשתות ויקי (data-tid) + היררכיה.
- **סיום:** SVG אינטראקטיבי (פריסת-כוחות ידנית, ~220 איטרציות, דטרמיניסטי). צומת=נושא (גודל לפי
  קישורים), לחיצה → TopicOpen, גלגלת=זום, גרירה=הזזה (viewBox).

### B9. ביום הזה לפני… — `js/features/onthisday/index.js`
- **כניסה:** כרטיס ב-`window.DASHBOARD_WIDGETS` (נבנה בכל רינדור של לוח הבקרה).
- **סיום:** בלוקים "לפני שבוע/חודש/שנה": מצב רוח+רפלקציה, הערות ונושאים (לפי updatedAt). אין
  נתונים → מחזיר null והכרטיס לא מופיע.

### B10. סקירה שבועית — `js/views/weekly-review/index.js` (view `weekly-review`, מפתח `weeklyReviews`)
- **כניסה:** מסך "🧭 סקירה שבועית".
- **סיום (v3):** סטטיסטיקות שבוע → **שלושה** שדות רפלקציה עשירים (👍 מה הלך טוב / 👎 מה לא הלך
  טוב / 🔧 מה לשפר) עם **תמונות** (הדבקה Ctrl+V או כפתור 🖼️, דרך `window.Editable` — אותה תשתית
  של ההערות) → שמירה לפי מפתח תחילת-שבוע → הרשומה מופיעה **מיד** ב"🗂️ ארכיון הסקירות" (כרטיס
  לכל שבוע, הנוכחי מסומן) → העברת משימות-יומן פתוחות +7 ימים.
- **ארכיון:** חיפוש מילת-מפתח על כל הסקירות + צ'יפים לסינון לפי שדה ("הצג רק את כל מה שהלך טוב").
  כרטיסי השבועות **מצומצמים כברירת מחדל** (תקציר שורה-לשדה + 🖼️ כשיש תמונה) עם כפתורי
  "🔍 הגדל" ⇄ "⤡ צמצם" ו-"✏️ עריכה" — עריכה נפתחת בתוך הכרטיס (שלושת השדות העשירים) ושומרת
  לאותו שבוע. מצב פתיחה/עריכה נשמר ברמת המודול (_expanded/_editing).
- **נתונים:** `weeklyReviews[weekKey] = {good, bad, improve, savedAt}` — הערכים HTML; רשומות
  ישנות בטקסט-פשוט נתמכות בתצוגה (`contentNode` מזהה לפי תגיות).
- **קריטי:** ⚠️ `Store.dateKey` הוא UTC — חישובי תאריך חייבים עיגון `T12:00:00` (חצות מקומית
  מתגלגלת יום אחורה). הכתיבה ל-`tasks` היא פעולת-משתמש מפורשת (חריגה מתועדת). תמונות נשמרות
  base64 בתוך maindoc — לצמצם תמונות-ענק.

### B11. מטריצת אייזנהאואר — `js/views/eisenhower/index.js` (view `eisenhower`, מפתח `eisenhower`)
- **כניסה:** מסך "🎯 מטריצת סדר יום". קורא todos פתוחות.
- **סיום:** 4 רבעים + מגירת לא-משויכות; גרירה משנה את מפת `{todoId: 'q1'..'q4'}` במפתח שלנו;
  שיוכים של משימות שנמחקו מנוקים אוטומטית. סימון-בוצע כותב ל-todos (פעולת משתמש מפורשת).

### B12. לוח שרטוט — `js/views/sketch/index.js` (view `sketch`)
- **כניסה:** מסך "✏️ לוח שרטוט". Canvas נקי: עט/קו/חץ/מלבן/עיגול/מחק, 6 צבעים, 3 עוביים, undo×30.
- **סיום:** "⬇ הורד PNG", או "📓 הוסף למחברת" → בורר נושא קיים / מחברת-חדשה-בשם → מוסיף
  `<figure class="nb-img"><img …>` ל-body של הנושא (התנהגויות התמונה של העורך נדבקות בפתיחה) → TopicOpen.

### B13. כרטיסיות זיכרון — `js/views/flashcards/index.js` (view `flashcards`, מפתח `flashcards`)
- **כניסה:** מסך "🧠 כרטיסיות זיכרון". כרטיסיה: `{id,q,a,topicId?,interval,due,reps,lapses}`.
- **סיום:** תרגול היומי (כל מה ש-due ≤ היום) → "ידעתי" מקפיץ למדרגה הבאה ב-[1,3,7,15,30,60,120],
  "לא ידעתי" → מחר. יצירה עם קישור אופציונלי לנושא; רשימת ניהול עם מחיקה.

### B14. מרכז הדגשות וחיפוש — `js/views/highlights/index.js` (view `highlights`)
- **כניסה:** מסך "🖍️ מרכז הדגשות". סורק topics+notes ב-DOMParser.
- **סיום (v2):** צ'יפי **מחברת ראשית** (כל המחברות / כל root / הערות; שיוך root ע"י טיפוס parentId
  עם cache) + שורת מילת-מפתח + שני מצבים:
  - 🖍️ **הדגשות** — אלמנטים עם background-color אינליין (לא לבן, הפנימי בלבד), מסוננים לפי מחברת+מילה.
  - 🔎 **כל הטקסט** — כל פסקת-עלה (p/li/h*/blockquote/td) שמכילה את המילה, חתוכה סביב המופע (220
    תווים), המילה מסומנת ב-`<mark>`, עד 10 לכל עמוד.
  כל תוצאה מקובצת לפי עמוד עם "פתח ↗" → TopicOpen / openNoteId. קריאה בלבד.

### B15. סריקת מסמך — `js/views/tools/doc-scan/index.js` (`window.Tools.docScan`, אריח בכלים)
- **כניסה:** מסך כלים → אריח "📷 סרוק מסמך למחברת". קלט: מצלמה (capture=environment בנייד) או קובץ.
- **סיום:** OCR מקומי (Tesseract heb+eng מהוונדור) → טקסט בעריכה → בורר: מחברת קיימת או
  **מחברת חדשה בשם מהמשתמש** → נוצר עמוד-בן (אייקון 📷, תגית 'סריקה') עם תמונה+טקסט → TopicOpen.
- **קריטי:** ⚠️ נתיבי הוונדור חייבים להיות מוחלטים (`location.origin + '/js/vendor/tesseract/'`) —
  נתיבים יחסיים נשברים בתוך ה-Worker של Tesseract (שוחזר ותוקן חי).

### B16. רשימת קריאה — `js/views/readinglist/index.js` (view `readinglist`, מפתח `readingList`)
- **כניסה:** מסך "🔖 רשימת קריאה". טופס: קישור (https מתווסף לבד) + כותרת (ברירת מחדל: הדומיין) + תגית.
- **סיום:** פריט בלשונית "לקריאה" → סימון ✓ מעביר ל"נקראו" → לחיצה על כותרת פותחת בטאב חדש.

> **CSS של B7–B16:** `css/features/extras.css` — בלוק מבודד לכל אחריות (מסומן בכותרת).
> B2–B6 — קובץ CSS נפרד לכל אחת תחת `css/features/`.

---

## חלק ג' — מה נשאר במאגר (לא נבנה, מאושר-מחקר)

שנה-בפיקסלים · מתאמי מצב-רוח · הישגים/רצפים · תקרות תקציב לקטגוריה · תנועות קבועות ·
לכידה מהירה (Ctrl+Shift+N) · חיפוש OCR בתוך תמונות המחברת · נעילת PIN · לוח עברי וחגים ·
דוח חודשי PDF · קנבן למשימות · חבר וירטואלי (Finch).
פרטי כל הצעה — בזיכרון הסוכן ובשיחות המחקר.

---

## חלק ד' — איך מוסיפים אחריות חדשה (תבנית)

1. תיקייה: `js/features/<שם>/` (ללא מסך) או `js/views/<שם>/` (עם מסך) או `js/views/tools/<שם>/` (כלי).
2. IIFE שחושף namespace אחד: `window.<שם>`.
3. מסך? `App.register('<id>', renderView)` + שורת SECTIONS ב-app.js (+הקפצת v).
4. כלי? אריח ב-stickers.js (+הקפצת v).
5. נתונים? מפתח ב-store-schema + מערכי האסרציה ב-firebase-sync. אודיו/בינארי גדול? IndexedDB משלך.
6. CSS: בלוק חדש ב-extras.css או קובץ נפרד תחת css/features (+link ב-index.html).
7. בוט: רשומת HELP ב-knowledge.js + עדכון overview ב-engine.js (+הקפצת v).
8. תיעוד: סעיף ב-ARCHITECTURE.md + סעיף כאן (כניסה/סיום/קריטי).
9. אימות חי על 7795 (node --check לכל JS, אפס שגיאות קונסול, ניקוי נתוני בדיקה) → קומיט אחד.
