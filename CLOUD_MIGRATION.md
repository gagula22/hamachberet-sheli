# תוכנית הטמעה לענן — הפלטפורמה החדשה על האתר החי (החלטה סופית)

> **הנחיית המשתמש (סופית, 2026-06-11):** ליישם את **הפלטפורמה** (הקוד/הפיצ'רים) על האתר
> שבענן. **כל הנתונים שבענן נשמרים כפי שהם.** נתוני האתר המקומי **לא מעניינים** — לא
> מעבירים אותם, לא ממזגים אותם, זורקים אותם.
>
> כלומר: **דרך A** — פריסת הקוד החדש אל `gagula22.github.io/hamachberet-sheli`, אותו
> Firebase project, אותם נתונים. ⛔ הביצוע רק לאחר "go" מפורש מהמשתמש.

---

## 1. העיקרון המרגיע: קוד ונתונים הם שתי מערכות נפרדות

- **GitHub (ה-repo)** מאחסן אך ורק את קוד האתר — HTML/JS/CSS. **אין בו שום נתון משתמש.**
- **הנתונים** חיים ב-Firestore (ענן) + עותק מקומי בדפדפן של המשתמש (origin של github.io).
- **החלפת קוד האתר אינה נוגעת בנתונים בכלל** — כמו להחליף את האפליקציה בטלפון: הקבצים שלך
  נשארים. דריסת נתונים יכולה לקרות רק אם הקוד החדש *כותב* משהו הרסני — ולכן §2.

## 2. למה הקוד החדש לא יפגע בנתוני הענן (הוכחה מהקוד)

- **המיזוג בטעינה ראשונה הוא תוסף-בלבד** (`firebase-sync.js`): רשימות — `mergeArrayById`
  טוען קודם את כל פריטי הענן ורק מוסיף מקומיים; **פריט-ענן בלי מקבילה מקומית לעולם לא
  נמחק**. אובייקטים — `{...cloud, ...local}`; הרגלים/slots — מיזוג-עומק.
- **ובמקרה שלנו זה כמעט לא רלוונטי:** האתר הפרוס רץ על origin של github.io, שבו ה"מקומי"
  של המשתמש הוא ממילא המראה של הענן (האתר החי הקיים). נתוני localhost:7795 לא נוסעים לשום
  מקום — origin אחר, אחסון אחר. **אפס סיכון לזליגת נתוני-בדיקה מקומיים לענן.**
- **4 מפתחות חדשים** (`readingList`, `flashcards` subcol · `eisenhower`, `weeklyReviews`
  maindoc): לא קיימים בענן → מאותחלים ריקים → נכתבים כתוספת. לא נוגעים בשום מפתח קיים.
- אין שום מיגרציה הרסנית בקוד: אין `delete`/`reset` אוטומטי על נתוני ענן.

## 3. מה כן דורש טיפול לפני/בזמן הפריסה (הצ'קליסט האמיתי)

1. **שינויים לא-מקומטים בתיקיית `מחברת/`** — יש שם עבודה שלא קומטה (index.html,
   notebook/editor.js, tools/pdf-to-word, תמונות wyckoff). לפני שמחליפים קוד חובה:
   `git -C מחברת status` → להחליט עם המשתמש מה גורלם (לקמט/לזרוק). לא לדרוס בעיוורון.
2. **דגל ה-sandbox** — להסיר `window.IMPROVED_SITE_SANDBOX = true;` מ-index.html (שורה
   ~98). זה השינוי היחיד שמפעיל Firebase. השומרים נשארים.
3. **Firestore Security Rules** — לוודא שהכללים מתירים את 2 ה-subcollections החדשים
   (`users/{uid}/readingList/**`, `users/{uid}/flashcards/**`). אם הכללים wildcard — מכוסה.
4. **Worker התמלול** — ה-CORS שלו כבר מאשר `https://gagula22.github.io` ✅ → תמלול-ענן
   (Whisper-Large, המסלול המהיר) יעבוד באתר החי **מיד, בלי פרוקסי**. `server.py` נשאר רק
   לפיתוח מקומי; `cloudBases()` מדלג על הפרוקסי אוטומטית כשה-hostname אינו localhost.
5. **sw.js** — שם ה-cache כבר הוקפץ (`mahberet-shell-v2`) → דפדפנים של משתמשים קיימים
   יקבלו את הקוד החדש ולא ייתקעו על ה-cache הישן.
6. **קבצים שלא שייכים לאתר חי** — לא להעלות: `server.py` (אין python ב-Pages; לא מזיק אבל
   מיותר), `HANDOFF.md`/`AUDIT.md` (תיעוד פנימי — לא סודי, החלטת נוחות בלבד).

## 4. נוהל הביצוע (כשהמשתמש אומר "go")

1. **גיבוי-אמת של הענן (מומלץ מאוד):** באתר החי הקיים — התחבר → "⬇ ייצוא" → JSON מלא.
   רשת ביטחון מוחלטת, גם אם §2 מבטיח אי-נגיעה.
2. **ריכוז השינויים הלא-מקומטים ב-`מחברת/`** (סעיף 3.1) — לקמט או לנטרל בהחלטת המשתמש.
3. בתוך ה-repo החי (`מחברת/`): צור branch `platform-fable5`.
4. העתק את עץ הקבצים של `אתר משופר עם fable 5/` על ה-branch (ללא `.git`, ללא `server.py`
   ותיעוד פנימי per 3.6) — `robocopy /MIR` ידרוס נכון כי זה branch מבודד.
5. הסר את דגל ה-sandbox (3.2). קומיט.
6. **אימות מקומי עם Firebase אמיתי:** הרץ את ה-branch על פורט 7788, המשתמש מתחבר בחשבון
   האמיתי — מאמתים: הנתונים הקיימים מופיעים, 22 מסכים, בוט, עריכה מסתנכרנת, אפס קונסול.
   (זה login אמיתי → רק המשתמש יכול לבצע; הסוכן מכין ומנחה.)
7. **בדיקת אי-נגיעה:** פריט ותיק שקיים בענן — עדיין שם אחרי ההתחברות; Firestore Console
   מראה שה-collections הקיימים לא השתנו (רק נוספו אולי 4 מפתחות ריקים).
8. merge ל-`main` → push → GitHub Pages מתעדכן.
9. **אימות חי על github.io:** §5.

## 5. אימות אחרי פריסה (על האתר החי)

- [ ] login עובד; הנתונים הוותיקים מופיעים מלאים (מחברות, הערות, תקציב, הרגלים…).
- [ ] כל 22 המסכים נטענים; הבוט עונה; Ctrl+K; מצב כהה; אפס שגיאות קונסול.
- [ ] עריכה מסתנכרנת דו-כיוונית (מכשיר שני / רענון).
- [ ] תמלול הקלטה באתר החי רץ דרך הענן הישיר (מהיר) — בלי שגיאת CORS.
- [ ] Firestore Console: אף collection קיים לא הצטמק; נוספו לכל היותר מפתחות/אוספים חדשים.
- [ ] rollback זמין: `git revert`/החזרת ה-branch הקודם ב-Pages — הקוד הישן חוזר, הנתונים
  כאמור לא נגעו בהם.

## 6. סטטוס החלטות

| החלטה | סטטוס |
|---|---|
| דרך ההטמעה | ✅ **A — על האתר החי**, אותו Firebase, אותם נתונים (הנחיית המשתמש) |
| נתוני המקומי | ✅ נזרקים — לא ממוזגים (הנחיית המשתמש) |
| גיבוי ענן לפני | ממתין לאישור (מומלץ מאוד) |
| גורל השינויים הלא-מקומטים ב-`מחברת/` | ממתין להחלטה בזמן הביצוע |
| מועד הביצוע | ⛔ ממתין ל"go" מפורש |
