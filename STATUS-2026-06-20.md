# 📍 סטטוס פרויקט "המחברת שלי" — 20.6.2026 (Handoff לסוכן הבא)

> מסמך זה מסכם את כל העבודה מהיום, המצב הנוכחי, הבעיה הפתוחה, ובדיוק מאיפה להמשיך.

## פרטי פרויקט
- **ריפו:** `gagula22/hamachberet-sheli` (branch: `main`)
- **חי באוויר:** https://gagula22.github.io/hamachberet-sheli (GitHub Pages מ-`main`)
- **תיקייה מקומית:** `C:\Users\ACER\Documents\claude AI\מחברת`
- **קומיט אחרון היום:** `76944b3` (השרשרת היום: 0db6abe → f6b9037 → acfcdd5 → eeea3c5 → 61fcc87 → f61e771 → 76944b3)
- **גיבוי מלא קיים אצל המשתמש:** `notebook-backup-2026-06-19.json` (40 מחברות, 203 תמונות). שחזור דרך כפתור "⬆ ייבוא" בתפריט הצדדי.

---

## ✅ מה נעשה היום — והכל חי באוויר
1. **PWA מלאה:** נוצרו `manifest.json`, `sw.js` (service worker), `icons/` (192/512/apple-touch/maskable). חובר ב-`index.html`, הוסר ה-`no-cache` האגרסיבי. עכשיו ניתן להתקנה + עבודה אופליין.
2. **כפתור "שמור לענן" מאוחד בעורך** (`editor.js`): אוחדו 💾+☁️ לכפתור אחד `saveToCloud`, חובר Ctrl+S.
3. **תמונות — תצוגה/איכות:**
   - תקרת A4 (680px): תמונה לעולם לא חורגת מרוחב A4 ולא נמתחת (זה תיקן טשטוש בהדבקה). `image.js` + `notebook.css`.
   - **ידיות גרירה לשינוי גודל** (עד A4): `image.js` + `notebook.css`.
   - `compressImage` נכתב מחדש (`utils.js` v21): דחיסה חכמה ≤2000px WebP/JPEG, יעד ~150KB לתמונה — כדי שדף יישאר מתחת ל-900KB ויסתנכרן.
4. **הדבקה מ-Word:**
   - הוסר `font-size` מ-whitelist של `cleanWordHtml` (`image.js`) — הדבקות עתידיות לא יביאו את גודל הגופן של המקור (13pt).
   - מירכוז תמונה בייצוא (`export.js`): inline במקום `display:block` (Word מתעלם מ-margin:auto).
5. **מודול `imgfit`** (`js/features/imgfit/index.js`): בטעינה, דוחס דפים מעל 900KB עד שייכנסו לגבול Firestore ויסתנכרנו.
6. **חוסן סנכרון:**
   - `syncTopics` כותב **כל דף בנפרד** (`firebase-sync.js` v30) — דף בעייתי לא חוסם את כל ה-batch (היה batch אטומי).
   - **תוקנה קריסה** (`firebase-sync.js` v31): `mergeByKey`/`mergeArrayById` קרס על מפתח maindoc שהוא אובייקט (לא מערך) → הפיל את כל מאזין הסנכרון. עכשיו מטופל ב-try/catch + טיפול באובייקטים.
7. **מודול `forcesync`** (`js/features/forcesync/index.js` v2): כפתור ירוק "🔎 בדוק והעלה לענן" בתפריט הצדדי — **בדיקת שרת אמיתית** (`get({source:'server'})`) + העלאת כל הדפים בנפרד, ומציג ספירת ענן לפני/אחרי + כשלים.

---

## 🟢 נסגר (20.6) — סנכרון/תצוגת מחברות חדשות
**הבעיה ("מציג רק עד שבוע 23") נפתרה ואומתה חי.** השורש: תצוגת המחברת לא צוירה מחדש כשנתוני הענן נחתו אחרי הרינדור הראשוני (פירוט מלא בסעיף "אובחן ותוקן" למטה). אחרי הפריסה (commit `369d7e4`, store v20 · app v32 · firebase-sync v32) — רשימת המחברות מציגה את כל ה-41 כולל שבוע 24/25 ו-14/15/17.6.26. אומת: ☁️ בענן 41, 💻 מקומי 41, אין מערך topics ישן ב-data/main. נותר רק לאמת על מכשיר הטלפון עצמו (רענון קשה / התקנת PWA מחדש).

## 🔴 הבעיה הפתוחה המרכזית — סנכרון לטלפון (היסטורי — ראה "נסגר" למעלה)
**סימפטום:** בטלפון מופיעות מחברות רק **עד "שבוע 23"**; החדשות (שבוע 24, 8.6, 9.6, 11.6) חסרות.

**מה כבר אובחן ותוקן:**
- (א) **Brave Shields במחשב חסם את `firestore.googleapis.com`** (`net::ERR_BLOCKED_BY_CLIENT` ב-console) → הכתיבות מהמחשב **לא הגיעו לשרת**, רק למטמון המקומי (שדיווח כוזב "נשמר"). **המשתמש כיבה את Brave Shields לאתר → תוקן.**
- (ב) קריסת `mergeArrayById` (סעיף 6 למעלה) — **תוקנה ב-v31.**

**מאומת (בדיקת שרת אמיתית):** אחרי כיבוי Brave Shields, כפתור "בדוק והעלה לענן" הראה: **"בענן אחרי: 41, נכשלו: 0"** (קריאה ישירה מהשרת). כלומר **צד הכתיבה תקין ב-100% — כל 41 המחברות באמת בענן.**

**מה שנשאר (כאן ממשיכים):** **הטלפון עדיין מציג רק 23** — גם אחרי ניקוי מטמון מלא. כלומר זה **לא** מטמון, אלא **באג בצד הקריאה/רינדור**.

### ✅ אובחן ותוקן (20.6 — ממתין לפריסה + אימות בטלפון)
**שורש הבעיה:** ה-`App` ב-`js/app.js` עושה `render()` רק על `hashchange` ועל `Store.ready()` — הוא **לא** נרשם ל-`Store.subscribe`. נתוני המחברות נכנסים ל-state אחרי הרינדור הראשוני (במובייל הענן מגיע מאוחר), אבל **תצוגת המחברת לא מצוירת מחדש** ונתקעת על מה שהיה בזיכרון בזמן הרינדור. שני פערים:
1. הסְנאפשוט הראשון של topics ב-`firebase-sync.js` משתמש ב-`Store._local` — שמעדכן state **בלי `emit()`** (בכוונה, למנוע הבהוב).
2. גם `Store._fromCloud` (שכן עושה `emit()`) לא הועיל, כי שום מאזין לא מרנדר את ה-route. הערת המפתח (~שורה 535) ש"_fromCloud → רינדור מחדש" הייתה **שגויה**.
בדסקטופ זה "עבד" כי הנתונים כבר היו מקומית ברינדור הראשון.

**התיקון (כירורגי, 3 קבצים, SoC — app.js בעלים של הרינדור):**
- `js/app.js` (v32): נוסף `App.onCloudUpdate()` שמרנדר מחדש את המסך הנוכחי כשנתוני ענן נוחתים, עם **הגנה מפני שיבוש עריכה** (אם contenteditable/input ממוקד — דוחה ומסמן `_cloudPending`); ומאזין `focusout` ב-`start()` שמבצע את הרינדור הדחוי בסיום העריכה. **opt-out** (`SELF_MANAGED_ROUTES = {tripmap, insights}`): views שמנהלים רענון-עצמי דרך `Store.subscribe` מדולגים — כדי לא לגרום רינדור-כפול (insights) או בניית-מחדש של מפת ה-3D (tripmap). שומר בדיוק את ההתנהגות הקיימת שלהם.
- `js/store.js` (v20): `_fromCloud` קורא ל-`App.onCloudUpdate()` (עדכוני זמן-אמת ממכשיר אחר).
- `js/firebase-sync.js` (v32): אחרי ש-`listenToCloud()` מסתיים (כל הסְנאפשוטים הראשונים בפנים) — קריאה אחת ל-`App.onCloudUpdate()`. **זה התיקון הישיר לטלפון.**

**אומת מקומית (preview 7788):** שוחזר הבאג (state=3, viewRows=0) → אחרי `onCloudUpdate()` viewRows=3. הגנת-העריכה: בזמן עריכה הרינדור נדחה (pending=true), וב-focusout מתבצע. אפס שגיאות קונסול.

### 👉 הצעד הבא המדויק
1. **לפרוס** (deploy .bat) — גרסאות ה-`?v=` כבר עודכנו ב-index.html (store v20 · app v32 · firebase-sync v32).
2. **לאמת בטלפון** אחרי הפריסה (Ctrl+Shift+R / התקנה מחדש של ה-PWA): המחברות (שבוע 24, 8.6, 9.6, 11.6) צריכות להופיע — כל ה-41.
3. אם **עדיין** 23 אחרי הפריסה → לחקור עמוק יותר את מאזין ה-topics ב-`firebase-sync.js` (merge שמשמיט) או את הקריאה מהשרת בטלפון.

---

## 🟡 משימות/בעיות פתוחות נוספות
1. **מחוון "נשמר" כוזב:** מציג "✓ נשמר" גם כשהמידע לא הגיע לשרת (Firestore מדווח הצלחה על כתיבה למטמון מקומי). לתקן: לאמת מול השרת (יש `FirebaseSync.verifyCloud()` שעושה קריאת `source:'server'`), ולהציג "ממתין/נחסם" כשלא באמת בענן. **המשתמש ביקש את זה במפורש.**
2. **זיהוי חסימת Firestore:** להוסיף זיהוי אוטומטי של `ERR_BLOCKED_BY_CLIENT` / כישלון `verifyCloud`, ולהציג אזהרה ברורה ("החיבור לענן חסום — כבה Brave Shields/חוסם פרסומות").
3. **שינוי גופן גורף:** בסימון כל הטקסט ובחירת גודל — לא משתנה (עובד רק על פסקה בודדת). באג בעורך (`editor.js`, `applyToSelection`/execCommand fontSize).
4. **גופן 13pt בייצוא של תוכן קיים:** ניקוי ההדבקה חל רק על הדבקות עתידיות. תוכן שכבר הודבק עדיין מייצא ב-13pt. אופציה: לכפות 11pt בייצוא (`export.js`, להסיר font-size מוטבע מגוף הטקסט).
5. **שורת משתמש (אימייל/שם) נחתכת במובייל** — תיקון CSS.
6. **תמונות בדפים כבדים:** לוודא ש-`imgfit` באמת מקטין אותם מתחת ל-900KB ושהאיכות מקובלת.
7. **מפת דרכים מהסקירה המקורית** (נמסרה למשתמש כקובץ Word "המחברת-שלי-סקירה-והמלצות"): חיפוש גלובלי אמיתי, איחוד מערכת המשימות, תזכורות, מצב כהה (חלקית קיים), תמיכת מגע במחברת.

---

## 🚀 מנגנון פריסה (CRITICAL — לסוכן הבא)
**הסוכן לא יכול לדחוף מהסביבה** (אין הרשאה למחוק `.git/index.lock`, ואין credentials). **המשתמש מריץ קובץ .bat** שעושה add/commit/push.
- **תהליך:** עורכים קבצים מקומית → **מעדכנים `?v=` ב-`index.html`** לכל JS/CSS ששונה (לעקיפת service worker) → המשתמש מריץ deploy bat → GitHub Pages בונה (~1-2 דק') → המשתמש מרענן **Ctrl+Shift+R**.
- בשורש הריפו יש כמה `deploy-*.bat`. תבנית עמידה: `del .git\index.lock` → `git reset` → `git add -A` → `git commit` → `git fetch` → `git merge origin/main` → `git push`. (אם השינוי ב-`firebase-sync.js` — לא לעשות `checkout --theirs` עליו.)

## ⚙️ מוזרויות סביבה (CRITICAL)
- **המאונט מקצץ קריאות/כתיבות של קבצים גדולים מהסביבה.** לקרוא אמין: `git --git-dir=.git --work-tree=. show HEAD:PATH` (עם `GIT_DISCOVERY_ACROSS_FILESYSTEM=1`). לערוך: ב-`/tmp`, ואז `cp` לריפו, ולוודא עם `cmp` (זהה בית-בבית) + `node --check`. **לא לסמוך על כלי Edit/Write לקבצים גדולים בלי אימות `cmp`.**
- `git` בסביבה דורש `GIT_DISCOVERY_ACROSS_FILESYSTEM=1` ו-`--git-dir=.git --work-tree=.`.
- יש `.git/index.lock` חוזר שהסביבה לא יכולה למחוק; ה-bat מוחק אותו (`del`).
- יש אזהרת packfile "improper chunk offset" שמופיעה לפעמים אבל git עדיין עובד.

## גרסאות קבצים נוכחיות (index.html)
### 🔧 גילוי נוסף (20.6) — ערוץ ה-Listen נחסם בטלפון (Brave/סלולרי)
אבחון חי (אינקוגניטו, מחובר): `get({source:'server'})` מחזיר **41** וה-Store של לקוח טרי = 41 (הרינדור תוקן עובד). **אבל** בקונסול מופיע `404` + `WebChannelConnection RPC 'Listen' stream transport errored` — כלומר ערוץ ה-**onSnapshot (Listen)** של Firestore נחסם. ה-`get()` החד-פעמי עובד, אבל האפליקציה טוענת נתונים דרך onSnapshot. הזיהוי-האוטומטי ו-`verifyCloud` מבוססי-`get` ולכן **לא תופסים** חסימה שמשפיעה רק על Listen. במחשב זה מתאושש; בטלפון (Brave + סלולרי) לא.
**תיקון (firebase-sync v33):** טריגר פר-מכשיר דרך כתובת URL — פתיחת `…/?lp=1` מדליקה את הדגל `mahberet.forceLP` (לפני קריאת הדגל ב-initSDK), כך ש-Firestore עובר ל-`experimentalForceLongPolling` שעובר את החסימה. `?lp=0` מכבה. בלי קונסול, בטוח (רק דגל מקומי). **הצעד בטלפון:** לפתוח `https://gagula22.github.io/hamachberet-sheli/?lp=1`, להמתין, ואז לרענן רגיל. fallback מובטח אם לא עוזר: ייצוא מהמחשב → ייבוא בטלפון.

### 🧠 חקירת 3-סוכנים (20.6) — שורש הבעיה בטלפון + תיקון
חקירה במקביל (פורנזיקת-git / מודל-נתונים / SW+תחבורה) הניבה מסקנה מתכנסת:
- **רגרסיה:** commit `89dfd53` (12.6) "Instant load: non-blocking" — העביר רינדור לפני טעינת הענן; הסְנאפשוט הראשון נכתב בשקט (`_local`). תוקן ב-`369d7e4` (onCloudUpdate).
- **שורש אמיתי לטלפון:** ה-topics נטענים **אך ורק** דרך `onSnapshot` (ערוץ ה-Listen). בטלפון (Brave/סלולרי) ערוץ ה-Listen **חסום** (`404` על `.../Listen/channel`); `get()` (REST) עובד אבל לא היה בשימוש ל-topics. לכן `?lp=1` לא עזר (אותו endpoint חסום). ה-SW נוקה מחשד (טאב פרטי = קוד טרי, עדיין 23).
- **מודל-נתונים:** ה-merge הוא union (לא חותך); ה-topics תמיד subcollection (לא היה מודל ישן).

**התיקון (firebase-sync v34, תיקון שורש):** נוסף **get({source:'server'}) גיבוי ל-topics** ב-`listenToCloud` (סעיף "2b") — קריאה חד-פעמית בטעינה, ממוזגת באיחוד ל-Store דרך `_fromCloud` (מרנדר, לא דוחף, לא מוחק). כשערוץ ה-Listen חסום — הרשימה עדיין נטענת דרך REST. + מודול אבחון קריאה-בלבד `js/features/syncdiag` (כפתור "🔬 אבחון ענן" בסיידבר): מציג Store/שורשים/יתומים/נגישים + שרת(get)/מטמון + גרסת-קוד/SW/long-polling — לאבחון בטלפון בלי קונסול. אומת ב-preview: הכפתור מופיע, ה-fallback קיים (union, בלי delete), קריאה-בלבד, אפס שגיאות.

firebase-sync v34 · syncdiag v1 · utils v21 · image v25 · editor v48 · export v54 · notebook.css v46 · imgfit v1 · forcesync v2 · store v20 · app v32
