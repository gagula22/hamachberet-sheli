# פרומפט פתיחה לסוכן — "המחברת שלי"

> העתק את כל הבלוק שמתחת לקו, הדבק לסוכן חדש, וכתוב את המשימה בשורה האחרונה.
> הפרומפט מפנה למסמכים החיים (UPDATES / ARCHITECTURE / PROJECTS) ולכן אינו מתיישן.

---

אתה עומד לעבוד על אפליקציית "המחברת שלי" — vanilla JS/CSS בלי build, סנכרון Firebase,
PWA עם Service Worker, מופצת ב-GitHub Pages (https://gagula22.github.io/hamachberet-sheli).
תיקיית העבודה: C:\Users\ACER\Documents\claude AI\מחברת

## 0. ראש נקי — לפני הכל

בוא למשימה הזו עם ראש צלול ונקי, בלי שאריות:
- אפס הנחות מסשנים/שיחות קודמים. אתה לא "זוכר" את הקוד — אתה קורא אותו עכשיו, כמות שהוא.
- כל טענה על התנהגות הקוד (שלך או ממסמך/זיכרון) מאומתת מול הקבצים הנוכחיים לפני שפועלים לפיה.
- אל תניח שתיקון קודם עדיין קיים ואל תניח שבאג ישן עדיין קיים — בדוק.
- המשימה הנוכחית בלבד: אל תגרור לתוכה שיפורים צדדיים שלא התבקשו. ראית משהו ראוי לתיקון?
  רשום אותו כהצעה בסוף — אל תיגע.

## 1. איך האתר בנוי — מפת האחריויות (10 תחומים, ~30 מיני-פרויקטים)

```
📓 המחברת שלי (פרויקט ראשי)
│
├── §1 ליבה ותשתית ─ P-01 מעטפת וניתוב · P-02 אחסון וסכימה · P-03 סנכרון ענן
│                     P-04 גיבוי · P-05 עיצוב וערכות נושא · P-06 ניווט וקיבוץ
│
├── §2 המחברת ────── P-10 עץ נושאים · P-11 עורך · P-12 מדיה ותמונות
│                     P-13 ייצוא Word/PDF · P-14 עיצוב המחברת
│
├── §3 יומן ומעקב ── P-20 יומן · P-21 משימות ומטריצה · P-22 בריאות והרגלים
│                     P-23 תקציב ומטרות · P-24 סקירה שבועית
│
├── §4 ידע ולכידה ── P-30 הערות · P-31 קשרים וגרף · P-32 הדגשות · P-33 כרטיסיות
│                     P-34 שרטוט · P-35 רשימת קריאה · P-36 סריקה · P-37 ביום הזה · P-38 פרומטים
│
├── §5 כלים ──────── P-40 מעטפת · P-41 Word↔PDF · P-42 פעולות PDF · P-43 תרגום PDF · P-44 תמלול
│
├── §6 קול ───────── P-50 הכתבה · P-51 הקלטות ותמלול
│
├── §7 עוזר וחיפוש ─ P-60 עוזר חכם · P-61 חיפוש Ctrl+K
│
├── §8 וויקוף ────── P-70 ניתוח עצמאי (חדש) · P-71 כרטיס Worker (ישן)
│
├── §9 טיולים ────── P-80 מפה וניווט · P-81 מתכנן טיולים
│
└── §10 דשבורד ───── P-90 לוח בקרה · P-91 תובנות · P-92 הגדרות
```

כל מיני-פרויקט (P-xx) הוא בעלים בלעדי של קבצים מסוימים. העיקרון המקודש של הפרויקט:
**הפרדת אחריות (SoC)** — שינוי בנושא מסוים נוגע אך ורק בקובץ שבבעלות אותו P-xx.
המשימה שלך שייכת לאחד (או שניים) מהכרטיסים האלה — זהה אותו לפני שאתה כותב שורה.

## 2. מה לקרוא, לפי הסדר, בלי לדלג

1. `UPDATES.md` — הפרוטוקול המחייב בראש הקובץ + הרשומה העליונה ביומן (המצב האחרון:
   מה נעשה, אילו גרסאות חיות, מה פתוח). זו נקודת ההמשך שלך.
2. `ARCHITECTURE.md` — מפת האחריות המלאה: מה כל קובץ עושה, טבלת "איפה משנים מה" (§2),
   מוסכמות חיווט (§3), ופרטי מימוש של כל תת-מערכת (§6–§15).
   חובה במיוחד אם נוגעים בעורך או ב-CSS: §14 (מלכודת הבחירה בעורך) ו-§15
   (מלכודות zoom/vh, ה-cascade של גובה המחברת, ו-Service Worker).
3. `PROJECTS.md` — כרטיס ה-P-xx של המשימה: קבצים בבעלותו, אזהרות ייעודיות, משימות
   פתוחות. וגם §0 — כללי מניעת התנגשות בין סוכנים.
4. `git log --oneline -5` — ודא שאתה מכיר את הקומיטים האחרונים ושאין עבודה מקבילה.

## 3. כללי עבודה שאינם ניתנים למשא ומתן

- **SoC:** לא בטוח מי הבעלים של השינוי? עצור וחפש בטבלת §2 של ARCHITECTURE לפני שכותבים.
- **כל פיצ'ר/תיקון = קומיט נפרד** עם הודעה מפורטת (בעיה → פתרון → אימות). בלי מרכאות
  כפולות או גרשים בהודעות קומיט (שוברים את PowerShell 5.1).
- **לפני כל דחיפה — צ'ק-ליסט חובה:**
  - `node --check` לכל JS ששונה.
  - הקפצת `?v=N` ב-index.html לכל JS/CSS ששונה — **גם אם כבר הוקפץ באותה סשן**
    (ה-Service Worker מגיש עריכה ישנה באותו URL!).
  - בדיקה חיה ב-preview מקומי (פורט 7788, דרך `.claude/launch.json`) — הפיצ'ר עובד
    לפי ההגדרה **וגם** מסכים שכנים לא נשברו. אפס שגיאות קונסול.
  - **אימות אמיתי, לא סינתטי:** בדוק את התרחיש שהמשתמש חווה (למשל בחירה בלי mouseup =
    מגע; `evt.defaultPrevented`), לא רק את המסלול הנוח. אירועים סינתטיים לא גוזלים
    פוקוס — אל תסיק מהם שכפתור עובד.
  - מפתח Store חדש? עדכן יחד: `store-schema.js` **וגם** מערך האסרציה ב-`firebase-sync.js`.
  - `git add` ממוקד לקבצים ששונו בכוונה בלבד (יש רעש CRLF — לעולם לא `git add -A`).
- **אחרי `git push origin main`:** המתן ~דקה ל-GitHub Pages ואמת שהקבצים החיים באתר
  באמת מכילים את השינוי (fetch עם פרמטר אקראי נגד cache).
- **תיעוד הוא חלק מהמשימה** — ראה סעיף 4. משימה בלי עדכון המסמכים = משימה לא גמורה.

## 4. חובת תיעוד — כל שינוי מתועד, בלי יוצא מן הכלל

הסוכן שיבוא אחריך לא רואה את השיחה הזו. הדרך היחידה שלו לדעת **מה** עודכן, **איך** ו**מתי**
היא המסמכים. לכן כל שינוי שאתה מבצע חייב להשתקף בקבצים הרלוונטיים לפני שאתה מסיים:

| מה השתנה | איפה מתעדים |
|---|---|
| כל שינוי, תמיד (קוד/עיצוב/תיעוד) | רשומה **בראש** היומן ב-`UPDATES.md` לפי התבנית: תאריך, P-xx, מה נעשה, מה הייתה הבעיה, איך נפתרה, קבצים + גרסאות `?v`, קומיטים, ואיפה ממשיכים |
| יכולת/התנהגות חדשה או משימה שנסגרה בכרטיס | כרטיס ה-P-xx ב-`PROJECTS.md` (שורת "בוצע" עם תאריך וקומיט) |
| שינוי מבני, מלכודת חדשה שהתגלתה, או כלל-מניעה חדש | `ARCHITECTURE.md` — הסעיף הרלוונטי (או סעיף חדש, כמו §14–§15 הקיימים) |
| כלל עבודה חדש שכל סוכן עתידי חייב להכיר | הקובץ הזה (`AGENT-PROMPT.md`) |

כללי ברזל:
- מעדכנים את המסמכים **באותה סשן**, לא "אחר כך" — עצירה בלי רשומת UPDATES = הסוכן הבא עיוור.
- ברשומה תמיד עונים על שלוש שאלות: **מה** עודכן · **איך** (הפתרון והסיבה) · **מתי** (תאריך + קומיט).
- תיקנת באג שנבע ממלכודת לא-מתועדת? התיעוד של המלכודת חשוב לא פחות מהתיקון — זה מה
  שמונע ממנה לחזור.
- עדכוני docs הם קומיט משלהם (או חלק מקומיט הפיצ'ר אם קטנים) — אבל לעולם לא נשכחים בחוץ.

## המשימה שלי עבורך:

[כאן תכתוב את המשימה]
