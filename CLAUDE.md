# CLAUDE.md — נטען אוטומטית בכל סשן Claude Code

אתה עובד על "המחברת שלי" — אתר חי: https://gagula22.github.io/hamachberet-sheli
האתר בנוי בשיטת אחריויות (Separation of Concerns): כל פיצ'ר = מיני-פרויקט עם גבולות קבצים.

## חובה לפני כל עבודה (בסדר הזה)
1. **`UPDATES.md`** — קרא את הפרוטוקול + הרשומה העליונה ביומן (איפה עצרנו, מה פתוח).
2. **`PROJECTS.md`** — אתר את כרטיס המיני-פרויקט (P-xx) של המשימה: קבצים בבעלות, אזהרות, משימות פתוחות. עבוד **רק** בקבצים שבבעלותו + נקודות-המגע של §0.
3. **`ARCHITECTURE.md`** — הסעיף הרלוונטי. נוגע בעורך/CSS? חובה §14–§15 (מלכודות מתועדות).
4. `git log --oneline -5` — מה קרה לאחרונה.

## חובה בסיום כל משימה (לפני שמסיימים את הסשן)
1. צ'ק-ליסט האימות העצמי המלא — ב-`UPDATES.md` (node --check, הקפצת `?v=N` ב-index.html, בדיקה חיה, אפס שגיאות קונסול, השוואה לציפיות).
2. `git add` **ממוקד** לקבצים שערכת בכוונה בלבד (יש רעש CRLF — לעולם לא `add -A` עיוור), קומיט אחד לפיצ'ר, push ל-main (= פריסה ל-GitHub Pages).
3. **עדכן את הפרויקט:** רשומת handoff חדשה בראש היומן ב-`UPDATES.md` (לפי התבנית שם) + עדכון סטטוס/משימות בכרטיס ה-P-xx ב-`PROJECTS.md` + סעיף ב-`ARCHITECTURE.md` אם השתנה משהו מבני.
4. אמת באתר החי אחרי ~1-2 דק' (Ctrl+Shift+R).

## כללי ברזל
- Stack: HTML/CSS/JS טהור, בלי build. תקשורת בין מודולים רק דרך namespaces על `window`.
- מפתח Store חדש = `store-schema.js` **וגם** אסרציית `firebase-sync.js` — תמיד יחד.
- אסור לערבב שני מיני-פרויקטים בקומיט אחד.
- פרומפט פתיחה מלא למשתמש: `AGENT-PROMPT.md`.
